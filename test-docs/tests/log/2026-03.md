# March 2026

## 2026-03-03

Initial test suite created. Schemas and documents written to exercise all
schema features: field types, section structure, link validation, auto-fix,
managed content, date headings, and `from_directory` title mode.

LSP integration confirmed working: OpenCode receives `textDocument/publishDiagnostics`
from `td lsp` after file open. Diagnostics surface in agent context after
write/edit operations.

Fixed broken link line numbers -- diagnostics now report the actual line of the
link rather than hardcoding `line: 0`. See [link-validation](../features/link-validation.md).
